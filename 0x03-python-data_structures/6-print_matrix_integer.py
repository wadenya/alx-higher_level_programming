#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    """prints a matrix of integers."""
    for y in matrix:
        for sl in y
        print("{:d}".format(sl), end=" " if sl != y[-1] else"")
        print()
